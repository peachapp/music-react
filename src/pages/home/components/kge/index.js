import React, { useState, useEffect, useCallback } from 'react';
import './index.less';

const Kge = (props) => {
  // props

  // data

  return <div className="kge-container">
    k歌页面
  </div>
};

export default React.memo(Kge);
