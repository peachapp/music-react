import React, { useEffect, useCallback } from 'react';
import './index.less';

const Boke = (props) => {
  // props

  // data

  return <div className="boke-container">
    播客页面
  </div>
};

export default React.memo(Boke);
