import React, { useEffect, useCallback } from 'react';
import './index.less';

const Wode = (props) => {
  // props

  // data

  return <div className="wode-container">
    我的页面
  </div>
};

export default React.memo(Wode);
