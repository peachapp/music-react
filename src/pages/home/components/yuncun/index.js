import React, { useState, useEffect, useCallback } from 'react';
import './index.less';

const Yuncun = (props) => {
  // props

  // data

  return <div className="yuncun-container">
    云村页面
  </div>
};

export default React.memo(Yuncun);
